# Incident Logs

This directory records operational incidents, deployment issues, and production fixes that are useful for future debugging.

Guidelines:

- Do not include bearer tokens, client secrets, passwords, or raw production connection strings.
- Include symptoms, root causes, resolution, verification, and follow-up work.
- Use `YYYY-MM-DD-short-title.md` filenames.
